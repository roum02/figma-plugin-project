{
  "name": "figma-plugin-project",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal TypeScript-powered Figma plugin scaffold",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "dotenv -e .env -- node build.js",
    "watch": "dotenv -e .env -- node build.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.85.0",
    "@figma/rest-api-spec": "^0.33.0",
    "dotenv-cli": "^7.4.4",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
